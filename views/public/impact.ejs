<%- include('../partials/header', { title: 'Impact – Ella Rises', currentUser }) %>

<section class="section">
  <div class="section-header">
    <h1 class="section-title">Impact & Outcomes</h1>
    <p class="section-subtitle">
      These summary metrics are powered by the data you manage in the admin portal.
    </p>
  </div>

  <div class="impact-highlight">
    <div class="impact-card">
      <h3>Participants Reached</h3>
      <p><strong><%= impactStats?.participantsCount || '---' %></strong></p>
    </div>
    <div class="impact-card">
      <h3>Average Recommendation Score</h3>
      <p><strong><%= impactStats?.avgRecommend || '--' %>/10</strong></p>
    </div>
    <div class="impact-card">
      <h3>STEAM Major Rate</h3>
      <p><strong><%= impactStats?.steamMajorRate || '--' %>%</strong></p>
    </div>
    <div class="impact-card">
      <h3>STEAM Job Rate</h3>
      <p><strong><%= impactStats?.steamJobRate || '--' %>%</strong></p>
    </div>
  </div>

  <div class="card">
    <h2>Milestones Over Time</h2>
    <!-- You can embed a Tableau dashboard or Chart.js here later -->
    <p class="muted">
      In the final version, this section will display our Tableau dashboard embedded into the website.
    </p>
  </div>
</section>

<%- include('../partials/footer') %>
